overwrite: true
schema: 'schema.docs.graphql'
documents: 'src/**/*.gql'
generates:
  src/generated/graphql.ts: # изменено здесь
    plugins:
      - typescript
      - typescript-react-apollo
